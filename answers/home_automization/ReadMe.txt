IMPORTANT:
    Although via the chat_id only you can/should be able to chat with this assistant,
    we SHOULD/MUST add a password validation or similar for home_automization (save in
    session (= maybe only ram, so as long as the bot runs --> should be enough because
    we will have a Docker Container without persistence.